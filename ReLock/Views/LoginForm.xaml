<UserControl x:Class="ReLock.Views.LoginForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:views="clr-namespace:ReLock.Views"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="400" d:DesignWidth="350">
    <StackPanel>
        <Ellipse Width="200" Height="200">
            <Ellipse.Fill>
                <ImageBrush x:Name="ImageSrc">
                </ImageBrush>
            </Ellipse.Fill>
        </Ellipse>
        <Label x:Name="Name" Content="JOHN DOE" HorizontalAlignment="Center" VerticalAlignment="Center"  FontSize="46" Margin="10" Foreground="White" FontFamily="Segoe UI Light"/>
        <views:CustomPasswordBox Width="250" Height="30" Margin="10"/>
        <Label x:Name="Prompt" Content="I forgot my password" HorizontalAlignment="Center" VerticalAlignment="Center"  FontSize="14" Margin="5" Foreground="White" FontFamily="Segoe UI"/>
    </StackPanel>
</UserControl>
