<UserControl x:Class="ReLock.Views.IdleLockScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:views="clr-namespace:ReLock.Views"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="768" d:DesignWidth="1028">
    <Grid ZIndex="0" Background="DimGray">
        <Grid ZIndex="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1.5*" />
                <RowDefinition Height="3*" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>
            <!-- Date and Time -->
            <views:DateTimePanel Grid.Row="1" Grid.Column="0" Height="Auto" Grid.RowSpan="2" VerticalAlignment="Bottom" Margin="20,0,0,100"/>
        </Grid>
        <!-- Background Image -->
        <Image Source="{Binding Background, UpdateSourceTrigger=PropertyChanged}" Stretch="Fill" />
    </Grid>
</UserControl>
